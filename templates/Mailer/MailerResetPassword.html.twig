<h3>
    Hello {{ user.getLastName() }}, please confirm your e-mail address.
</h3>

<p>
    Please confirm your e-mail address by clicking
    {# <a href="{{ url('account_login') }}">this</a> #}
    <a href="{{ url('password_reset', {'token': user.getConfirmationToken()}) }}">this</a>
    link or, type the code inside the form: <b>{{ user.getConfirmationToken() }}</b>
</p>

